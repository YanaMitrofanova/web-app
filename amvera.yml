meta:
  environment: node
  toolchain:
    name: npm
    version: 22

build:
  command: cd frontend && npm install && npm run build
  artifacts:
    key: "frontend/dist"/*
    Value: /

run:
  # Устанавливаем легкий сервер для раздачи статических файлов на порту 80
  command: npx serve -s . -p 80

