meta:
  environment: node
  toolchain:
    name: npm
    version: 22

build:
  # Используем правильное поле для кастомных команд сборщика
  additionalCommands:
    - cd frontend && npm install && npm run build
  artifacts:
    "frontend/dist": /

run:
  # Оставляем пустым для автоматического запуска веб-сервера
  command: ""
